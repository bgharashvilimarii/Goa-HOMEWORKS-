// const numbers = [1, 2, 3, 4];

// const sum = numbers.reduce((acc, current) => {
//   return acc + current;
// }, 0);

// console.log(sum); // 10

const numbers = [1, 2, 3, 4];
const sum = numbers.reduce((acc, current) => {
  return acc + current;
}, 0);

// Take a starting value (initialValue)
// Loop through the array
// Pass the accumulator and current value to a function
// Save the returned result back into the accumulator
// Return the final accumulator

function redclone(array,acc,curvalue) {
    for(let i = 0; i < array.length; i++) {
        
    }
}